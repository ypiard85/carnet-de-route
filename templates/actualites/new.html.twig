{% extends 'base.html.twig' %}

{% block title %}COREEGO | Ajouter un actualité{% endblock %}

{% block body %}
    <section class="container top_margin">
           {{ form_start(form) }}
                    {{ form_row(form.name) }}
                <div class="mb-3">
                    {{ form_row(form.title) }}
                </div>
                {{ form_row(form.content) }}
                <button class="btn btn-primary mt-3">Créer</button>
           {{ form_end(form) }}
    </section>
{% endblock %}